# screenfetch

Creating a simple 'fetch' for arch, with C++ and JSON for management.

---

The idea is to develop a simple fetch with animations in C++ and editable parameters via JSON, aimed at Hyprland environments.
